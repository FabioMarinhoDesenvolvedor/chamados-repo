# Sessão 2026-06-25

## Bloco 1 — Setup inicial
- Criada estrutura de memória do projeto (docs/memory/)
- Definidas decisões iniciais: PostgreSQL, TypeScript full-stack, monorepo com NPM workspaces
- Modelagem inicial do banco: users, departments, tickets
- Definida regra de prioridade: f(complexity, department.priority_weight)
- Arquitetura de pastas para backend e frontend documentada

## Bloco 2 — Fechamento da stack e regras de negócio
- Stack fechada:
  - Backend: NestJS + Prisma
  - Frontend: React + Vite + React Router + TanStack Query + shadcn/ui (Tailwind)
  - Auth: JWT stateless (guard de auth + guard de role)
- Regras de negócio definidas (ver architecture/business-rules.md):
  - 2 perfis: admin (atende/gerencia) e user (abre). assigned_to sempre admin.
  - Visibilidade: user vê só os seus; admin vê todos.
  - Prioridade por matriz fixa (proposta — pendente validação final do Fabio).
  - Acompanhamento: histórico de status + comentários (2 tabelas novas).
- Banco atualizado: ticket_status_history, ticket_comments; assigned_to = sempre admin.
- Decisões documentadas: use-nestjs, use-prisma, use-react-vite, auth-jwt.

## Bloco 3 — Scaffolding do MVP 1.0 (executado)
- Matriz de prioridade APROVADA por Fabio. Plano de scaffolding aprovado. Pasta-lixo removida.
- Monorepo criado e compilando (shared ✓ / api ✓ / web ✓):
  - `@chamados/shared`: enums (union types) + tipos/DTOs.
  - `@chamados/api`: NestJS + Prisma. Módulos auth (JWT), users, departments, tickets.
    PriorityService + priority.matrix (matriz aprovada). Guards JwtAuthGuard + RolesGuard.
    schema.prisma com 5 tabelas. Seed com admin/user/depts/chamados.
  - `@chamados/web`: React+Vite+Tailwind+TanStack Query. Login, Dashboard (filtros + responsivo
    tabela/cards), Novo chamado, Detalhe (status/atribuição/histórico/comentários), telas admin
    (Usuários, Departamentos). AppShell com sidebar→drawer mobile.
- Endpoints: POST /auth/login; GET /users/me, GET/POST /users (admin); GET/POST /departments;
  POST/GET /tickets, GET /tickets/:id, PATCH /tickets/:id/status (admin),
  PATCH /tickets/:id/assign (admin), POST /tickets/:id/comments.
- Gotchas registrados: shared-cjs-rollup (web consome shared do source; api usa dist),
  windows-brace-expansion. Decisão: enum-strategy.
- npm install OK. Build dos 3 pacotes OK. Prisma Client gerado.

## Pendências
- Subir o banco e validar runtime: Docker Desktop NÃO estava rodando nesta sessão; não foi
  possível rodar migrate/seed. Rodar: `docker compose up -d` → `npm run db:migrate` (nome: init)
  → `npm run db:seed` → `npm run dev` e testar o fluxo ponta a ponta.
- 22 vulnerabilidades no `npm audit` (3 low/12 mod/7 high) — revisar sem `--force` antes de prod.
- Refresh token (auth) ficou fora do MVP — reavaliar.
- Testes automatizados ainda não criados.

## Próximo passo
- Validar runtime (migrate/seed/dev) com o banco no ar e ajustar o que aparecer.
- Depois: testes (unit do PriorityService, e2e de auth/tickets) e CI.
